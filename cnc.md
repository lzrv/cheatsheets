# CNC notes / cheatsheet

## Installation

### Local / dev (with Kind)

* Create kind cluster
```bash
kind create cluster
```

* Clone CNC repo
* Run install script
```bash
cd local-dev
./deploy-cnc.sh
```
